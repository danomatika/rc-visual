#
# control commands preceded by '!'
# commands: group, endgroup, sprite, endsprite

!group
phatnphunky1  # phatnphunky1

    !sprite
        bar1 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	------------
	------------
	*-----------

        color 255 0 0 255     # R G B A, values 0 - 255
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

    !sprite
        bar2 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	------------
	--*---------
	--*---------

        color 255 153 0 255     # orange
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

    !sprite
        bar3 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	----*-------
	----*-------
	----*-------

        color 255 255 0 255     # yellow
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

	!sprite
        bar4 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------*-----
	------*-----
	------*-----
	------*-----

        color 50 255 0 255     # green
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

	
	!sprite
        bar5 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	---------*--
	------------
	------------
	------------

        color 0 0 255 255     # blue
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

!sprite
        bar6 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	-----------*
	------------
	------------
	------------

        color 0 0 0  255     # black
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

!endgroup



!group
phatnphunky2  # phatnphunky2

	!sprite
        back 1 1 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	*
        color 255 0 0 255     # R G B A, values 0 - 255
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite    


	!sprite
        bar1b 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	*-----------
	*-----------
	*-----------
	*-----------

        color 255 0 0 255     # R G B A, values 0 - 255
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

    !sprite
        bar2b 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	--*---------
	--*---------
	--*---------

        color 255 153 0 255     # orange
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

    !sprite
        bar3b 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	------------
	----*-------
	----*-------

        color 255 255 0 255     # yellow
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

	!sprite
        bar4b 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	------------
	------------
	------*-----

        color 50 255 0 255     # green
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

	
	!sprite
        bar5b 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	------------
	------------
	---------*--

        color 0 0 255 255     # blue
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

!sprite
        bar6b 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	------------
	------------
	-----------*

        color 90 0 255 255     # purple
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

!endgroup


!group
phatnphunky3  # phatnphunky3

    !sprite
        bar1a 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	------------
	------------
	*-----------

        color 255 0 0 255     # R G B A, values 0 - 255
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

    !sprite
        bar2a 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	------------
	--*---------
	--*---------

        color 255 153 0 255     # orange
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

    !sprite
        bar3a 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	----*-------
	----*-------
	----*-------

        color 255 255 0 255     # yellow
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

	!sprite
        bar4a 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------*-----
	------*-----
	------*-----
	------*-----

        color 50 255 0 255     # green
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

	
	!sprite
        bar5a 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	---------*--
	------------
	------------
	------------

        color 0 0 255 255     # blue
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

!sprite
        bar6a 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	-----------*
	------------
	------------
	------------

        color 90 0 255 255     # purple
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

	!sprite
        bar6a 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	------------
	-----------*
	------------

        color 0 255 200 255     # lt blue
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

	!sprite
        bar6a 12 4 1  # sprite name, width, height, num of frames

        # the sprite itself, '*' = filled pixel, '-' = empty pixel
	
	------------
	------------
	------------
	-----------*

        color 255 0 150 255     # magenta
        size 640 480    # width height, how big the sprite is
        pos 0 0 # x y, where the sprites upper left corner is
        animate 0   # should the sprite automatically animate?, '1' = yes, '0' = no
    	visible 0	# hide
    !endsprite

!endgroup

